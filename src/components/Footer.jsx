import React from 'react';

const Footer = ( {date_now} ) => (
        <footer>
            <p>Todos los derechos reservados &copy; {date_now}</p>
        </footer>
      );

 export default Footer;

